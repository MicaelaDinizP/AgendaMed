<resources>
    <!-- Tema overlay personalizado -->
    <style name="ThemeOverlay.CustomMenuStyle" parent="Widget.MaterialComponents.ActionBar.PrimarySurface">
        <item name="android:background">@drawable/welcome_gradient</item>
        <item name="android:textSize">20dp</item>
    </style>
    <style name="ThemeOverlay.CustomStyle" parent="ThemeOverlay.MaterialComponents.Light">
        <!-- Substituir o estilo do botão -->
        <item name="buttonStyle">@style/CustomButtonStyle</item>
        <!-- Adicione outras personalizações se necessário -->
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/buttonBaseDeepPurple</item>
    </style>

    <!-- Exemplo de um estilo de botão personalizado -->
    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">16dp</item>
    </style>
</resources>
